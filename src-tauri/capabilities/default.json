{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions. Plugins (dialog, opener, global shortcut) are driven from Rust commands, so the webview only needs core IPC + events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
