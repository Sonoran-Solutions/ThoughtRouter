{
  "type": "object",
  "additionalProperties": false,
  "required": ["links", "new_projects"],
  "properties": {
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["atom", "project", "confidence", "reason"],
        "properties": {
          "atom": { "type": "string" },
          "project": { "type": "string" },
          "confidence": { "type": "number" },
          "reason": { "type": "string" }
        }
      }
    },
    "new_projects": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["atom", "name", "description"],
        "properties": {
          "atom": { "type": "string" },
          "name": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    }
  }
}
